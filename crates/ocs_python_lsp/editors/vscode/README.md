# OCS Python LSP — VS Code Extension

Minimal VS Code extension that activates on Python files and launches
`ocs_lsp_bridge.py` as the Python language server for each temporary
workspace created by `PYTHONEDIT`.

See the top-level `PLUGIN.md` for the full architecture.
