[package]
name = "plugin-template-api3"
version = "0.1.0"
edition = "2021"
description = "V3 test plugin for OpenCADStudio plugin lifecycle tests."
license = "GPL-3.0-only"

[lib]
crate-type = ["cdylib"]

[dependencies]
ocs_plugin_api = { path = "../ocs_plugin_api", features = ["host"] }
